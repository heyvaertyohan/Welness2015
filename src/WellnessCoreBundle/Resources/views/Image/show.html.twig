{% extends 'WellnessCoreBundle::layout_backend.html.twig' %}

{% block breadcrumb %}
    <li><a href="#">{{ 'Liste de service'|trans }}</a></li>
{% endblock %}

{% block title %}
    {{ parent() }}
{% endblock %}


{% block contentadmin %}

<table class="record_properties">
    <tbody>
    <tr>
        <th>Id</th>
        <td>{{ entity.id }}</td>
    </tr>
    <tr>
        <th>Position</th>
        <td>{{ entity.position }}</td>
    </tr>
    <tr>
        <th>Type</th>
        <td>{{ entity.type }}</td>
    </tr>
    <tr>
        <th>Path</th>
        <td>{{ entity.path }}</td>
    </tr>
    <tr>
        <th>Description</th>
        <td>{{ entity.description }}</td>
    </tr>
    </tbody>
</table>

<ul class="record_actions">
    <li>
        <a href="{{ path('admin_image') }}">
            <i class="icon-backward"> </i> {{ 'Retour à la liste'|trans }}
        </a>
    </li>
    <li>
        <a href="{{ path('admin_image_edit', { 'id': entity.id }) }}">
            <i class="icon-edit"> </i> {{ 'Editer'|trans }}
        </a>
    </li>
    {{ form(delete_form) }}
    {% endblock %}
