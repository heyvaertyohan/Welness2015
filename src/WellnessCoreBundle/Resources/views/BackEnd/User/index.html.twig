{% extends 'WellnessCoreBundle::layout_backend.html.twig' %}

{% block breadcrumb %}
    <li><a href="#">{{ 'Liste des utilisateurs'|trans }}</a></li>
{% endblock %}

{% block titlepadding %}
    <h2>{{ 'Liste des utilisateurs'|trans }}</h2>
{% endblock %}

{% block contentadmin %}

    <table class="table table-striped table-hover records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Name</th>
                <th>Telnumber</th>
                <th>Usertype</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('admin_user_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.name }}</td>
                <td>{{ entity.telnumber }}</td>
                <td>{{ entity.usertype }}</td>
                <td>
                    <a href="{{ path('admin_user_show', { 'id': entity.id }) }}"><i class="icon-book"></i></a>
                    <a href="{{ path('admin_user_edit', { 'id': entity.id }) }}"><i class="icon-edit"></i></a>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('admin_user_new') }}">
                <i class="icon-building">     {{ 'Ajouter'|trans }}</i>
            </a>
        </li>
    </ul>
    {% endblock %}
