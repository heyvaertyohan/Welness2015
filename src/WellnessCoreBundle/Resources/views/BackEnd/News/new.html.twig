{% extends '::base.html.twig' %}

{% block body -%}
    <h1>News creation</h1>

    {{ form(form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('news') }}">
            <i class="icon-backward"></i> {{ 'Retour à la liste'|trans }}
        </a>
    </li>
</ul>
{% endblock %}
